class Counter {
public:
    int score(int value) const {
        int x = 10;
        int y = 2;
        int z = 3;
        return value + x + y + z;
    }

    double score(double value) const {
        return value;
    }
};
