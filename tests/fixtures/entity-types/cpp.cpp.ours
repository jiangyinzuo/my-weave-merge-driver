#define LIMIT 10

namespace demo {
struct Point {
    int x;
};

class Renderer {
public:
    int render(int value) const { return value + LIMIT + 1; }
};

enum Color { Red, Blue };

int calculate(int value) {
    return value + LIMIT;
}
}
